{
  "name": "join-weather-data",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "clean": "rm -rf ./build",
    "make_build_dir": "mkdir -p build/src",
    "precopy_resources": " npm run clean && npm run make_build_dir",
    "copy_resources": "cp index.js package.json build/src",
    "prebuild": " npm run copy_resources",
    "build": "cd build/src && npm install --production",
    "postbuild": " npm run zip",
    "zip": "cd build/src && zip -r ../function.zip ./*"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.471.0",
    "fast-csv": "^2.5.0"
  }
}
